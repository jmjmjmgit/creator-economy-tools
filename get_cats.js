const fs = require('fs');

// We need to fetch the original data.js from Git to see what ALL_CATEGORIES actually looked like
// just to be 100% sure we didn't miss any obscure categories or formatting
